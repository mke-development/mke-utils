package team.mke.utils

import java.time.ZoneId

/**
 * UTC timezone instance
 * */
val utcZoneId: ZoneId = ZoneId.of("Z")

/**
 * Yekaterinburg, Russia timezone instance (UTC+5)
 * */
val yekaZoneId: ZoneId = ZoneId.of("+05:00")
